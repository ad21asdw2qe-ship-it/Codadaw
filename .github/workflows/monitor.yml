name: SA-MP Monitor
on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Download servers.json from FTP
        run: |
          curl -o servers.json "http://a1189822.xsph.ru/servers.json"
        
      - name: Check server online and update JSON
        run: |
          python3 -c "
          import socket, struct, json, datetime
          
          # Проверяем онлайн
          try:
              sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
              sock.settimeout(5)
              packet = b'SAMP' + bytes([193,23,220,249]) + struct.pack('<H', 1171) + b'i'
              sock.sendto(packet, ('193.23.220.249', 1171))
              response, addr = sock.recvfrom(4096)
              if response[10] == 105:
                  players = struct.unpack('<H', response[12:14])[0]
                  print(f'🟢 {datetime.datetime.now().strftime(\"%H:%M:%S\")} | Онлайн: {players} игроков')
              else:
                  players = 0
                  print('❌ Неверный ответ')
          except Exception as e:
              players = 0
              print(f'❌ Ошибка: {e}')
          
          # Обновляем servers.json
          with open('servers.json', 'r') as f:
              servers = json.load(f)
          
          for server in servers:
              server['online'] = players
          
          with open('servers.json', 'w') as f:
              json.dump(servers, f, indent=4)
          
          print('✅ servers.json обновлен')
          "
          
      - name: Upload to FTP
        run: |
          curl -T servers.json --user blaze@a1189822:OsoW_123 "ftp://a1189822.xsph.ru/public_html/servers.json"
